[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python bot.py"

[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.deploy]
start = "python bot.py"
